Android
  - Retrofit + OkHttp + Gson
  - Glide

Swift
  - AFNetwoking(Alamofire) - X
  - Alamofire
  - Kingfisher

Android
  - Kotlin
  - Java

iOS Library
  - Objective C
  => Bridge
  - Swift

의존성 관리
  1. Cocoapods - Swift / ObjC
  2. carthage  - Swift
  3. Swift Package Manager - Swift

C/C++ : Library(함수 + 클래스)
   - 정적 라이브러리
   - 동적 라이브러리

Windows: DLL
    C:\Windows\System32\
Linux  : so(Shared Object)
macOS  : dylib


library
  foo.so
  void foo(int a, int b) { return a + b;}

  foo.h  
  void foo(int a, int b);

Library + Header + Etc
    => Framework


library math.library
- static library
   prog + math.library => a.out
   (2K)    (1K)        => 3K

- dynamic library
   prog + math.library.2 => a.out
   (2K)                     2K

   prog2
   (3K)

Visual Studio
   Solution
     - Project
     - Project   

Android Studio
   Project
     - Module
     - Module
Xcode
   Workspace(SampleApp.xcworkspace)
     - Project(SampleApp.xcodeproj)
     - Project(Pods.xcodeproj)
      - Alamofire

$ pod init
$ pod install















